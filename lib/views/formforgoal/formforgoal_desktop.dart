part of formforgoal_view;

class _FormforgoalDesktop extends StatelessWidget {
  const _FormforgoalDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FormforgoalDesktop')),
    );
  }
}
