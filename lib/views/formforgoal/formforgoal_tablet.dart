part of formforgoal_view;

class _FormforgoalTablet extends StatelessWidget {
  const _FormforgoalTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FormforgoalTablet')),
    );
  }
}
