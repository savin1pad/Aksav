
part of graphview_view;

class _GraphViewTablet extends StatelessWidget {
  const _GraphViewTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Graph'),
      ),
      body: const Center(
        child: Text('GraphViewTablet'),
      ),
    );
  }
}