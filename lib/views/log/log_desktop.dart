part of log_view;

class _LogDesktop extends StatelessWidget {
  const _LogDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LogDesktop')),
    );
  }
}
