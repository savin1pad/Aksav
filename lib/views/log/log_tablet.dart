part of log_view;

class _LogTablet extends StatelessWidget {
  const _LogTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LogTablet')),
    );
  }
}
