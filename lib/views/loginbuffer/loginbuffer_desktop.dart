part of loginbuffer_view;

class _LoginbufferDesktop extends StatelessWidget {
  const _LoginbufferDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LoginbufferDesktop')),
    );
  }
}
