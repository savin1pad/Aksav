part of loginbuffer_view;

class _LoginbufferTablet extends StatelessWidget {
  const _LoginbufferTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LoginbufferTablet')),
    );
  }
}
