
import 'package:flutter/material.dart';

class NoteseditscreenDesktop extends StatelessWidget {
  const NoteseditscreenDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit Note'),
      ),
      body: Container(),
    );
  }
}