import 'package:flutter/material.dart';

class NoteseditscreenTablet extends StatelessWidget {
  const NoteseditscreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit Note'),
      ),
      body: Container(),
    );
  }
}
