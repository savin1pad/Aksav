part of notesview_view;

class _NotesViewTablet extends StatelessWidget {
  const _NotesViewTablet();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Notes'),
      ),
      body: const Center(
        child: Text('NotesViewTablet'),
      ),
    );
  }
}
