part of rememberour_view;

class _RememberourDesktop extends StatelessWidget {
  const _RememberourDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('RememberourDesktop')),
    );
  }
}
