part of survey_1_view;

class _Survey1Desktop extends StatelessWidget {
  const _Survey1Desktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey1Desktop')),
    );
  }
}
