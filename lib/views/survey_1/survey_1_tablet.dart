part of survey_1_view;

class _Survey1Tablet extends StatelessWidget {
  const _Survey1Tablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey1Tablet')),
    );
  }
}
