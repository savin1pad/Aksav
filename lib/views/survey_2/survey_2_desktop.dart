part of survey_2_view;

class _Survey2Desktop extends StatelessWidget {
  const _Survey2Desktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey2Desktop')),
    );
  }
}
