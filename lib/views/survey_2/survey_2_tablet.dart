part of survey_2_view;

class _Survey2Tablet extends StatelessWidget {
  const _Survey2Tablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey2Tablet')),
    );
  }
}
