part of survey_3_view;

class _Survey3Desktop extends StatelessWidget {
  const _Survey3Desktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Survey3Desktop',
        ),
      ),
    );
  }
}
