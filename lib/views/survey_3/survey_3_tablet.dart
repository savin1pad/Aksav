part of survey_3_view;

class _Survey3Tablet extends StatelessWidget {
  const _Survey3Tablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey3Tablet')),
    );
  }
}
