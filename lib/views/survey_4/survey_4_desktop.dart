part of survey_4_view;

class _Survey4Desktop extends StatelessWidget {
  const _Survey4Desktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey4Desktop')),
    );
  }
}
