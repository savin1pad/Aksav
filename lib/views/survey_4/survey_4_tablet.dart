part of survey_4_view;

class _Survey4Tablet extends StatelessWidget {
  const _Survey4Tablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Survey4Tablet')),
    );
  }
}
