part of survey_stack_view;

class _SurveyStackDesktop extends StatelessWidget {
  const _SurveyStackDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SurveyStackDesktop')),
    );
  }
}
