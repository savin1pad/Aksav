part of survey_stack_view;

class _SurveyStackTablet extends StatelessWidget {
  const _SurveyStackTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SurveyStackTablet')),
    );
  }
}
